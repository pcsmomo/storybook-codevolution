import Button from '@mui/material/Button';

export default {
  title: 'Mui/Button',
  component: Button,
};

export const Success = () => (
  <Button variant="contained" color="primary">
    Success
  </Button>
);
